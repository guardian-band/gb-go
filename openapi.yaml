openapi: 3.0.3
info:
  title: Guardian Band API
  description: HTTP API for the Guardian Band application.
  version: 0.1.0

servers:
  - url: http://localhost:3000
    description: Local development server

paths:
  /api/register:
    post:
      summary: Register a user
      operationId: registerUser
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: User successfully registered
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          description: Invalid request
        '409':
          description: User already exists
        '500':
          description: Internal server error

  /api/login:
    post:
      summary: Log in a user
      operationId: loginUser
      tags:
        - Authentication
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthRequest'
      responses:
        '200':
          description: User successfully logged in
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '400':
          description: Invalid request
        '401':
          description: Invalid credentials

  /api/protected:
    get:
      summary: Sample protected endpoint requiring JWT
      operationId: getProtected
      tags:
        - Protected
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Access granted
          content:
            text/plain:
              schema:
                type: string
                example: Access granted to protected endpoint!
        '401':
          description: Missing, invalid, or expired JWT token

  /api/upload:
    post:
      summary: Upload a file to S3 / MinIO storage
      operationId: uploadFile
      tags:
        - Storage
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: File to upload
      responses:
        '201':
          description: File uploaded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UploadResponse'
        '400':
          description: Invalid request or missing file
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error or storage failure

  /api/vitals:
    post:
      summary: Store a vital packet in Redis Stream and Cache
      operationId: storeVitals
      tags:
        - Vitals
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VitalPacket'
      responses:
        '201':
          description: Vitals successfully recorded
        '400':
          description: Invalid vital data range or invalid payload
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - device does not belong to the patient
        '500':
          description: Internal server error

  /api/vitals/{userId}/latest:
    get:
      summary: Retrieve the latest vitals record for a user
      operationId: getLatestVitals
      tags:
        - Vitals
      security:
        - BearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Vitals successfully retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VitalPacket'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized access (missing or invalid JWT token)
        '404':
          description: Patient not found or unauthorized access (hidden status)
        '500':
          description: Internal server error or storage failure

  /api/profile:
    get:
      summary: Get the authenticated user's patient profile
      operationId: getProfile
      tags:
        - Profile
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Profile successfully retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatientProfile'
        '401':
          description: Missing, invalid, or expired JWT token
        '404':
          description: Profile not found for this user
        '500':
          description: Internal server error
    put:
      summary: Create or update the authenticated user's patient profile
      operationId: putProfile
      tags:
        - Profile
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatientProfile'
      responses:
        '200':
          description: Profile successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatientProfile'
        '400':
          description: Invalid request or payload validations failed
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error

  /api/account:
    get:
      summary: Retrieve the authenticated user's account role and facts
      operationId: getAccount
      tags:
        - Account
      security:
        - BearerAuth: []
      responses:
        '200':
          description: User account successfully retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
        '401':
          description: Missing, invalid, or expired JWT token
        '404':
          description: User account not found
        '500':
          description: Internal server error
    put:
      summary: Create or update the authenticated user's account role and facts
      operationId: putAccount
      tags:
        - Account
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserAccount'
      responses:
        '200':
          description: User account successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserAccount'
        '400':
          description: Invalid request body or role value
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error

  /api/documents:
    get:
      summary: Get the authenticated user's document catalog list
      operationId: getDocuments
      tags:
        - Documents
      security:
        - BearerAuth: []
      responses:
        '200':
          description: List of documents successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DocumentCatalog'
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error
    post:
      summary: Create a new document in catalog and upload file to S3
      operationId: createDocument
      tags:
        - Documents
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file (PDF, JPEG, PNG, max 10MB)
                kind:
                  type: string
                  enum: [report, prescription]
                  description: Kind of document
                title:
                  type: string
                  description: Title of the document
              required: [file, kind, title]
      responses:
        '201':
          description: Document successfully created and uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  objectKey:
                    type: string
                  versionId:
                    type: string
                  message:
                    type: string
        '400':
          description: Invalid request body, oversize file, or unsupported file format
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error or storage failure

  /api/documents/{documentId}:
    get:
      summary: Get detailed catalog metadata for a specific document
      operationId: getDocumentDetail
      tags:
        - Documents
      security:
        - BearerAuth: []
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Document catalog details successfully retrieved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentCatalog'
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - document does not belong to the user
        '404':
          description: Document not found
        '500':
          description: Internal server error

  /api/documents/{documentId}/content:
    get:
      summary: Stream authorized document content from S3
      operationId: getDocumentContent
      tags:
        - Documents
      security:
        - BearerAuth: []
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Document stream successfully retrieved
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - document does not belong to the user
        '404':
          description: Document not found
        '500':
          description: Internal server error
    put:
      summary: Update the content of an existing document
      operationId: updateDocumentContent
      tags:
        - Documents
      security:
        - BearerAuth: []
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file (PDF, JPEG, PNG, max 10MB)
              required: [file]
      responses:
        '200':
          description: Document content successfully updated
        '400':
          description: Invalid request body, oversize file, or unsupported file format
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - document does not belong to the user
        '404':
          description: Document not found
        '500':
          description: Internal server error

  /api/medication-catalog:
    get:
      summary: Retrieve list of common medications in the catalog
      operationId: getMedicationCatalog
      tags:
        - Medications
      security:
        - BearerAuth: []
      parameters:
        - name: search
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Medication catalog list successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MedicationCatalogItem'
        '401':
          description: Missing or invalid token
        '500':
          description: Internal server error

  /api/medications:
    get:
      summary: Retrieve the patient's active medication plans
      operationId: getMedications
      tags:
        - Medications
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Active medication plans successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MedicationResponse'
        '401':
          description: Missing or invalid token
        '500':
          description: Internal server error
    post:
      summary: Create a new medication plan for the patient
      operationId: createMedicationPlan
      tags:
        - Medications
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMedicationRequest'
      responses:
        '201':
          description: Medication plan successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MedicationResponse'
        '400':
          description: Invalid inputs or missing prescription reference
        '401':
          description: Missing or invalid token
        '403':
          description: Forbidden - prescription document belongs to another user
        '500':
          description: Internal server error

  /api/medications/{medicationId}:
    delete:
      summary: Soft-delete an active medication plan
      operationId: deleteMedicationPlan
      tags:
        - Medications
      security:
        - BearerAuth: []
      parameters:
        - name: medicationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Medication plan successfully deleted
        '401':
          description: Missing or invalid token
        '403':
          description: Forbidden - medication plan belongs to another patient
        '404':
          description: Medication plan not found
        '500':
          description: Internal server error

  /api/medications/{medicationId}/taken:
    post:
      summary: Record that a dose has been taken now
      operationId: takeMedicationDose
      tags:
        - Medications
      security:
        - BearerAuth: []
      parameters:
        - name: medicationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dose successfully recorded and next schedule returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TakenResponse'
        '401':
          description: Missing or invalid token
        '403':
          description: Forbidden - medication plan does not belong to the user
        '404':
          description: Active medication plan not found
        '500':
          description: Internal server error

  /api/sos/incidents:
    post:
      summary: Trigger a new SOS incident and alert emergency contacts
      operationId: postSOSIncident
      tags:
        - SOS
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SOSIncidentRequest'
      responses:
        '201':
          description: SOS incident successfully created and alerts sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SOSIncidentResponse'
        '400':
          description: Invalid request payload or coordinates validation failed
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error

  /api/sos/incidents/{incidentId}/all-clear:
    post:
      summary: Process an all-clear signal for the active SOS incident
      operationId: allClearSOSIncident
      tags:
        - SOS
      security:
        - BearerAuth: []
      parameters:
        - name: incidentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: All clear processed successfully and notification sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SOSAllClearResponse'
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - SOS incident belongs to another user
        '404':
          description: SOS incident not found
        '500':
          description: Internal server error

  /api/notification-endpoints/{endpointId}:
    put:
      summary: Register or update a notification endpoint
      operationId: putNotificationEndpoint
      tags:
        - Notifications
      security:
        - BearerAuth: []
      parameters:
        - name: endpointId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - channel
                - token
              properties:
                channel:
                  type: string
                  enum: [push, sms]
                  example: "push"
                token:
                  type: string
                  example: "device-token-123"
      responses:
        '200':
          description: Notification endpoint successfully registered or updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  channel:
                    type: string
                  token:
                    type: string
                  active:
                    type: boolean
                  updatedAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid request parameters or body
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - endpoint belongs to another user
        '500':
          description: Internal server error
    delete:
      summary: Remove a notification endpoint registration
      operationId: deleteNotificationEndpoint
      tags:
        - Notifications
      security:
        - BearerAuth: []
      parameters:
        - name: endpointId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Notification endpoint successfully deleted
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - endpoint belongs to another user
        '404':
          description: Notification endpoint not found
        '500':
          description: Internal server error

  /api/emergency-access/tokens:
    post:
      summary: Generate a short-lived QR access token for the patient
      operationId: createEmergencyAccessToken
      tags:
        - Emergency Access
      security:
        - BearerAuth: []
      responses:
        '201':
          description: QR token successfully created
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    example: "4d7c83ba9a8c7b8d8f9e0a1b2c3d4e5f"
                  expiresAt:
                    type: string
                    format: date-time
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error

  /api/emergency-access/redeem:
    post:
      summary: Redeem patient QR token to establish emergency session
      operationId: redeemEmergencyToken
      tags:
        - Emergency Access
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
              properties:
                token:
                  type: string
                  example: "4d7c83ba9a8c7b8d8f9e0a1b2c3d4e5f"
      responses:
        '200':
          description: Token redeemed successfully and session established
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessionId:
                    type: string
                    format: uuid
                  expiresAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid or expired token
        '401':
          description: Missing, invalid, or expired JWT token
        '500':
          description: Internal server error

  /api/emergency-access/sessions/{sessionId}/medical-card:
    get:
      summary: Retrieve patient emergency medical card for active session
      operationId: getEmergencyMedicalCard
      tags:
        - Emergency Access
      security:
        - BearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Medical card retrieved successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmergencyMedicalCard'
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - session expired, revoked, or responder mismatch
        '404':
          description: Session not found
        '500':
          description: Internal server error

  /api/emergency-access/sessions/{sessionId}:
    delete:
      summary: Revoke emergency access session (patient only)
      operationId: revokeEmergencySession
      tags:
        - Emergency Access
      security:
        - BearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Session successfully revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "emergency access session revoked successfully"
        '401':
          description: Missing, invalid, or expired JWT token
        '403':
          description: Forbidden - patient mismatch
        '404':
          description: Session not found
        '500':
          description: Internal server error

  /api/patient-link-invitations:
    post:
      summary: Create a patient-approved monitoring invitation
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMonitoringInvitationRequest'
      responses:
        '201':
          description: Invitation created; the opaque token is returned once
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitoringInvitationResponse'
        '400':
          description: Invalid invitation options
        '401':
          description: Missing, invalid, or expired JWT token

  /api/patient-link-invitations/redeem:
    post:
      summary: Redeem a single-use monitoring invitation
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedeemMonitoringInvitationRequest'
      responses:
        '201':
          description: Relationship created or reactivated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonitoringRelationshipResponse'
        '400':
          description: Invalid, expired, used, or self-link invitation
        '401':
          description: Missing, invalid, or expired JWT token

  /api/monitoring/patients:
    get:
      summary: List patients explicitly approved for monitoring
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Active can-monitor relationships for the authenticated member
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MonitoringPatient'
        '401':
          description: Missing, invalid, or expired JWT token

  /api/patient-relationships:
    get:
      summary: List registered relationships owned by the authenticated patient
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Patient-owned registered relationships
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PatientRelationship'
        '401':
          description: Missing, invalid, or expired JWT token

  /api/patient-relationships/{relationshipId}:
    delete:
      summary: Revoke a monitoring relationship owned by the patient
      security:
        - BearerAuth: []
      parameters:
        - name: relationshipId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Relationship revoked
        '401':
          description: Missing, invalid, or expired JWT token
        '404':
          description: Relationship not found for this patient

  /api/emergency-contacts:
    get:
      summary: List external emergency contacts
      security:
        - BearerAuth: []
      responses:
        '200':
          description: External contacts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EmergencyContact'
    post:
      summary: Create an external emergency contact
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmergencyContactRequest'
      responses:
        '201':
          description: Contact created
        '400':
          description: Invalid contact
        '409':
          description: Contact already exists

  /api/emergency-contacts/{contactId}:
    patch:
      summary: Update an external emergency contact
      security:
        - BearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmergencyContactPatch'
      responses:
        '200':
          description: Contact updated
        '404':
          description: Contact not found for this patient
        '409':
          description: Another contact already uses this phone number
    delete:
      summary: Delete an external emergency contact
      security:
        - BearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Contact deleted
        '404':
          description: Contact not found for this patient

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    AuthRequest:
      type: object
      required:
        - phoneNumber
        - password
      properties:
        phoneNumber:
          type: string
          description: E.164 biçimindeki telefon numarası
          example: "+905551234567"
        password:
          type: string
          format: password

    AuthResponse:
      type: object
      required:
        - token
        - userId
      properties:
        token:
          type: string
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        userId:
          type: string
          format: uuid
          example: 9660ff3a-a370-4f46-9eab-daba77da0f96

    UploadResponse:
      type: object
      properties:
        file_key:
          type: string
          example: 4a2b9f80-1234-5678-9abc-def012345678.png
        message:
          type: string
          example: file uploaded successfully

    RegisterRequest:
      type: object
      required:
        - phoneNumber
        - password
      properties:
        phoneNumber:
          type: string
          description: E.164 biçimindeki telefon numarası
          example: "+905551234567"
        password:
          type: string
          format: password
          minLength: 8
          example: secure-password
        displayName:
          type: string
          maxLength: 200

    RegisterResponse:
      type: object
      required:
        - id
        - phoneNumber
      properties:
        id:
          type: string
        phoneNumber:
          type: string
        displayName:
          type: string

    CreateMonitoringInvitationRequest:
      type: object
      required: [relationship, kind, canMonitor]
      properties:
        relationship:
          type: string
        kind:
          type: string
          enum: [family, clinician, other]
        canMonitor:
          type: boolean
          enum: [true]
        isEmergencyContact:
          type: boolean
          default: false
        expiresInSeconds:
          type: integer
          minimum: 1
          maximum: 3600

    MonitoringInvitationResponse:
      type: object
      required: [token, expiresAt]
      properties:
        token:
          type: string
          description: Opaque single-use invitation token; shown only at creation.
        expiresAt:
          type: string
          format: date-time

    RedeemMonitoringInvitationRequest:
      type: object
      required: [token]
      properties:
        token:
          type: string

    MonitoringRelationshipResponse:
      type: object
      required: [id, patientId, memberUserId, relationship, kind, canMonitor, isEmergencyContact]
      properties:
        id:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        memberUserId:
          type: string
          format: uuid
        relationship:
          type: string
        kind:
          type: string
          enum: [family, clinician, other]
        canMonitor:
          type: boolean
        isEmergencyContact:
          type: boolean

    MonitoringPatient:
      type: object
      required: [id, patientId, displayName, relationship, bloodType, birthDate]
      properties:
        id:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        displayName:
          type: string
        name:
          type: string
        relationship:
          type: string
        bloodType:
          type: string
        birthDate:
          type: string
          format: date

    EmergencyContactRequest:
      type: object
      required: [displayName, phone, relationship]
      properties:
        displayName:
          type: string
        phone:
          type: string
        relationship:
          type: string
        isPrimary:
          type: boolean

    EmergencyContact:
      allOf:
        - $ref: '#/components/schemas/EmergencyContactRequest'
        - type: object
          required: [id]
          properties:
            id:
              type: string
              format: uuid

    EmergencyContactPatch:
      type: object
      properties:
        displayName:
          type: string
          maxLength: 200
        phone:
          type: string
          maxLength: 32
        relationship:
          type: string
          maxLength: 64
        isPrimary:
          type: boolean

    PatientRelationship:
      type: object
      required: [id, memberUserId, memberDisplayName, relationship, kind, canMonitor, isEmergencyContact, active, createdAt]
      properties:
        id:
          type: string
          format: uuid
        memberUserId:
          type: string
          format: uuid
        memberDisplayName:
          type: string
        relationship:
          type: string
        kind:
          type: string
          enum: [family, clinician, other]
        canMonitor:
          type: boolean
        isEmergencyContact:
          type: boolean
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        revokedAt:
          type: string
          format: date-time
          nullable: true

    VitalPacket:
      type: object
      required:
        - recordedAt
      properties:
        userId:
          type: string
          nullable: true
        heartRate:
          type: integer
          description: Kalp atış hızı (bpm)
          example: 72
        bloodOxygen:
          type: integer
          description: Kandaki oksijen oranı (SpO₂ yüzdesi)
          example: 98
        recordedAt:
          type: string
          format: date-time
        deviceSn:
          type: string
          example: "band-xyz-123"
        readingId:
          type: string
          example: "read-999"

    UserAccount:
      type: object
      required:
        - role
      properties:
        role:
          type: string
          enum: [patient, family, doctor]
          example: "doctor"
        roleFacts:
          type: object
          additionalProperties: true
          example:
            email: "dr@hastane.com"
            doctorHospital: "Acibadem"
            doctorSpecialty: "Cardiology"

    PatientProfile:
      type: object
      properties:
        birthDate:
          type: string
          format: date
          example: "1995-04-12"
        bloodType:
          type: string
          example: "A+"
        criticalFacts:
          type: object
          additionalProperties: true
          example:
            allergies:
              - penicillin
            chronicConditions:
              - asthma
        timezone:
          type: string
          example: "Europe/Istanbul"
        bandIdentifier:
          type: string
          nullable: true
          example: "GB-12345"

    DocumentCatalog:
      type: object
      required:
        - id
        - kind
        - title
        - contentType
        - currentVersionId
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        kind:
          type: string
          enum: [report, prescription]
        title:
          type: string
          example: "Kan Analiz Raporu"
        contentType:
          type: string
          example: "application/pdf"
        currentVersionId:
          type: string
          example: "v1.0"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    MedicationCatalogItem:
      type: object
      required:
        - id
        - name
        - strength
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: "Parol"
        strength:
          type: string
          example: "500mg"

    CreateMedicationRequest:
      type: object
      required:
        - name
        - frequencyHours
      properties:
        name:
          type: string
          example: "Parol"
        strength:
          type: string
          example: "500mg"
        instructions:
          type: string
          example: "Günde 2 kez aç karnına"
        frequencyHours:
          type: integer
          enum: [6, 8, 12, 24]
          example: 8
        prescriptionDocumentId:
          type: string
          format: uuid
          nullable: true

    MedicationResponse:
      type: object
      required:
        - id
        - name
        - frequencyHours
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        strength:
          type: string
        instructions:
          type: string
        frequencyHours:
          type: integer
        lastTakenAt:
          type: string
          format: date-time
          nullable: true
        nextScheduledFor:
          type: string
          format: date-time
          nullable: true

    TakenResponse:
      type: object
      required:
        - nextScheduledFor
      properties:
        nextScheduledFor:
          type: string
          format: date-time

    SOSIncidentRequest:
      type: object
      properties:
        latitude:
          type: number
          format: double
          minimum: -90
          maximum: 90
          example: 41.0082
        longitude:
          type: number
          format: double
          minimum: -180
          maximum: 180
          example: 28.9784
        accuracyMeters:
          type: number
          format: double
          minimum: 0
          example: 15.5
        address:
          type: string
          example: "İstanbul, Türkiye"

    SOSIncidentResponse:
      type: object
      required:
        - id
        - patientId
        - status
        - startedAt
      properties:
        id:
          type: string
          format: uuid
        patientId:
          type: string
          format: uuid
        status:
          type: string
          example: "active"
        latitude:
          type: number
          format: double
          nullable: true
        longitude:
          type: number
          format: double
          nullable: true
        accuracyMeters:
          type: number
          format: double
          nullable: true
        address:
          type: string
          nullable: true
        startedAt:
          type: string
          format: date-time

    SOSAllClearResponse:
      type: object
      required:
        - id
        - status
        - allClearAt
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          example: "active"
        allClearAt:
          type: string
          format: date-time
          nullable: true

    EmergencyMedicalCard:
      type: object
      required:
        - bloodType
        - criticalFacts
        - medications
      properties:
        bloodType:
          type: string
          example: "A+"
        criticalFacts:
          type: object
          example: {"allergies": ["Penicillin"]}
        medications:
          type: array
          items:
            $ref: '#/components/schemas/EmergencyMedication'
        latestVitals:
          $ref: '#/components/schemas/EmergencyLatestVitals'
          nullable: true

    EmergencyMedication:
      type: object
      required:
        - name
        - strength
        - instructions
      properties:
        name:
          type: string
          example: "Aspirin"
        strength:
          type: string
          example: "100mg"
        instructions:
          type: string
          example: "Take once daily"

    EmergencyLatestVitals:
      type: object
      required:
        - heartRate
        - bloodOxygen
        - recordedAt
      properties:
        heartRate:
          type: integer
          example: 72
        bloodOxygen:
          type: integer
          example: 98
        recordedAt:
          type: string
          format: date-time
